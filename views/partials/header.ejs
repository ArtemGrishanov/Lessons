<header>
    <div class="header-inner">
        <div class="container">
            <div class="navbar navbar_ clearfix">
                <div class="navbar-inner">
                    <div class="clearfix">
                        <h1 class="brand"><a href="<%= properties.root + pages.indexPage %>"><img src="<%= properties.root %>i/logo.png"
                                                                          alt="" style="height:34px;"></a><span>coderock</span></h1>

                        <div class="block-sign">
                            <% if (isLoggedIn != true) { %>
                            <a href="<%= properties.root + pages.pageLogin %>">Войти</a>&nbsp; |&nbsp; <a href="<%= properties.root + pages.pageSignUp %>">Регистрация</a>
                            <% } %>
                            <% if (isLoggedIn == true) { %>
                            <a href="<%= properties.root + pages.profilePage %>"><%=user.local.email%></a>
                            <% } %>
                        </div>
                        <div class="clearfix"></div>
                        <div class="menu-search">
                            <div class="nav-collapse nav-collapse_ collapse">
                                <ul class="nav sf-menu clearfix sf-js-enabled">
                                    <li class="active"><a href="<%= properties.root + pages.indexPage %>">Стартовая</a></li>
                                    <li class="sub-menu"><a href="<%= properties.root + pages.servicesPage %>">Все курсы</a>
                                    </li>
                                    <li><a href="<%= properties.root + pages.profilePage %>">Мой профиль</a></li>
                                    <li><a href="<%= properties.root + pages.contactsPage %>">Контакты</a></li>
                                    <li><a href="<%= properties.root + pages.faqPage %>">FAQ</a></li>
                                </ul>
                            </div>
                            <form id="search" action="search.php" method="GET" accept-charset="utf-8">
                                <input type="text" value="" name="s">
                                <a href="#" onclick="document.getElementById('search').submit()"></a>
                            </form>
                            <div class="clearfix"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</header>